<h1>New Order</h1>
<div class="row">
   
<div class="col-lg-2">

<%= render 'form', order: @order %>



TOTAL ORDER AMOUNT
<%= render  @cart %>
<%= button_to "Go Back To Cart", "/carts/#{session[:cart_id]}", :method => :get, :form_class => "my_class" %>

</div>
</div>
        <% if current_user!=nil %>
        <% if current_user.admin? %>
<%= link_to 'Back', orders_path %>
<%end%>
<%end%>
